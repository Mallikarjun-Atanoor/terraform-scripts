📄 README.md
IAM User Management with Terraform (CSV-Driven)
📌 Overview

This project uses Terraform to automatically create AWS IAM users by reading user details from a CSV file.
Users are tagged, login profiles are created, and they can be grouped based on attributes like department or job role.